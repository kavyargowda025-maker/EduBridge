{
  "name": "edubridge-student-diary",
  "version": "1.0.0",
  "description": "EduBridge Mobile App & School Management System for Horizon International School",
  "main": "backend/dist/server.js",
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "concurrently -n \"backend,frontend\" -c \"blue,green\" \"npm run start --prefix backend\" \"npm run dev --prefix frontend\"",
    "build": "npm run build --prefix frontend && npm run build --prefix backend",
    "start": "node backend/dist/server.js"
  },
  "devDependencies": {
    "@vercel/node": "^5.10.1",
    "concurrently": "^8.2.2"
  }
}
